plugins {
    id 'cpp-application'
}

application {
    targetMachines = [machines.linux.x86_64]
}

tasks.withType(CppCompile).configureEach {
    compilerArgs.addAll([
        '-std=c++17',
        '-Werror',
        '-Wall',
        '-Wextra',
        '-Wpedantic',
        '-Wformat=2',
    ])
}
